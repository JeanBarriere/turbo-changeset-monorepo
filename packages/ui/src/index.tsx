import { Theme, Button, Badge, Blockquote, AlertDialog } from '@radix-ui/themes';

import '@radix-ui/themes/styles.css';

/**
 * We export the Theme and Button components from the Radix UI library.
 */
export const UI = {
    Theme,
    AlertDialog,
    Button,
    Badge,
    Blockquote,
};
